


define(["require", "hoist"], function(require) {

    var __dirname = "modules/bindable/lib/core",
    __filename    = "modules/bindable/lib/core/utils.js",
    module        = { exports: {} },
    exports       = module.exports,
    define        = undefined,
    window        = exports,
    global        = window;

    

    // Generated by CoffeeScript 1.6.2
(function() {
  var hoist;

  hoist = require("hoist");

  exports.tryTransform = function(transformer, method, value) {
    if (!transformer) {
      return value;
    }
    return transformer[method].call(transformer, value);
  };

  exports.transformer = function(options) {
    if (typeof options === "function") {
      options = {
        from: options,
        to: options
      };
    }
    if (!options.from) {
      options.from = function(value) {
        return value;
      };
    }
    if (!options.to) {
      options.to = function(value) {
        return value;
      };
    }
    return {
      from: hoist.map(options.from),
      to: hoist.map(options.to)
    };
  };

}).call(this);


    return module.exports;
});