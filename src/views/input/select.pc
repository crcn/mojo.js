<select name="{{ name }}" data-bind="{{ onChange: _changeSelect(event), preventDefault: false }}">

  <option value="nil" selected="{{ nothingSelected ? 'selected' : undefined }}">
    {{ selectLabel }}
  </option>

  {{ html: sections.selectList }}

</select>